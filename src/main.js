/* main.js
   =========================================
   Entry point
   ========================================= */

const app = document.getElementById("app");

function init() {
  app.innerHTML = `
    <h1>New Game</h1>
    <p>Ready to build something awesome.</p>
  `;
}

init();
